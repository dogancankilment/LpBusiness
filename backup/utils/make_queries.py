from home.models import *
from django.shortcuts import get_object_or_404


def models_count(request,object_id):
    # current_obj = get_object_or_404(Test.objects.select_related(), pk=object_id)
    return True
